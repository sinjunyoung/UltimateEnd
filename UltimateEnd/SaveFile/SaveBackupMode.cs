namespace UltimateEnd.SaveFile
{
    public enum SaveBackupMode
    {
        NormalSave,      // 일반 세이브만 (.srm, .sav 등)
        SaveState,       // 세이브 스테이트 (.state*)
        Both             // 둘 다
    }
}